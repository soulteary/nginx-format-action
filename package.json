{
  "name": "nginx-format-action",
  "version": "1.0.0",
  "private": true,
  "description": "Check or format Nginx configuration files in GitHub Actions",
  "license": "Apache-2.0",
  "packageManager": "bun@1.4.0",
  "scripts": {
    "check": "bun build ./dist/index.js --target=node --outdir=.bun-check && rm -rf .bun-check",
    "test": "bun test"
  },
  "engines": {
    "bun": ">=1.4.0"
  }
}
